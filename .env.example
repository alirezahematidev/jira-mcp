# --- Jira credentials (required) ---------------------------------------------
# Self-hosted Jira host (no trailing slash) and a personal access token (PAT).
JIRA_HOST=https://works.digikala.com
JIRA_PAT=your-personal-access-token
